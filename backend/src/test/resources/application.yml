server:
  port: 8888

spring:
  application:
    name: produto-cadastro-listagem

  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none

cors:
  originPatterns: http://localhost:5173